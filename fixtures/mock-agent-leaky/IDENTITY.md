# Leaky agent (mock)

Identity for a project that accidentally left a credential inside a SHAREABLE path. The consent
gate must abort the whole upload loudly — this fixture exists only to prove that it does.
